{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "esbuild (compile)",
      "type": "shell",
      "command": "conda activate build; npm run compile",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": [],
      "group": { "kind": "build", "isDefault": true },
      "presentation": { "reveal": "silent" }
    }
  ]
}
